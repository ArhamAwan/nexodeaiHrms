[build]
  command = "pnpm install && pnpm build"
  publish = ".next"
  base = "."

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--version"

[functions]
  node_bundler = "esbuild"
